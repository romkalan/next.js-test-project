function RestaurantsLayout({children}) {
    return (
        <html lang="en">
        <body>
        Restaurants Layout
        {children}
        </body>
        </html>
    );
}

export default RestaurantsLayout;
